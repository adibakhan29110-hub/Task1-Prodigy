# backend/docker-compose.yml
version: "3.8"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
    environment:
      - MODEL_DIR=/app/outputs/gpt2-finetuned
    restart: unless-stopped
